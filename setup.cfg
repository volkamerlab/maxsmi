# Helper file to handle all configs

[coverage:run]
# .coveragerc to control coverage.py and pytest-cov
omit =
    # Omit the tests
    */tests/*
    # Omit generated versioneer
    maxsmi/_version.py
    versioneer.py
    setup.py
    examples/
    maxsmi/full_workflow.py
    maxsmi/full_workflow_optimal.py
    maxsmi/prediction_unlabeled_data.py
    maxsmi/pytorch_models.py
    maxsmi/parser_default.py
    maxsmi/pytorch_training.py
    maxsmi/pytorch_evaluation.py

[yapf]
# YAPF, in .style.yapf files this shows up as "[style]" header
COLUMN_LIMIT = 119
INDENT_WIDTH = 4
USE_TABS = False

[flake8]
# Flake8, PyFlakes, etc
max-line-length = 119

[versioneer]
# Automatic version numbering scheme
VCS = git
style = pep440
versionfile_source = maxsmi/_version.py
versionfile_build = maxsmi/_version.py
tag_prefix = ''

[aliases]
test = pytest
