name: maxsmi
channels:
  - conda-forge
dependencies:
    # Base depends
  - python>=3.7
  - pip
  - jupyter
  - jupyterlab
  - rdkit>=2018.09.2.0
  - scikit-learn
  - requests
  - tqdm
  - pytorch
  - matplotlib
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

